{
  "name": "react-practical-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf build && mkdir build",
    "start": "node ./dist/src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-babel": "babel src/**/*.js -d dist",
    "build": "babel src/*.js -d dist && babel src/**/*.js -d dist && babel src/**/**/*.js -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-preset-env": "^1.7.0",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.6.7"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "nodemon": "^1.19.1"
  }
}
